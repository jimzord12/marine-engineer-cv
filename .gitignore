# Real candidate data and private entry points never enter the public repo.
private/

# Build and test outputs. Every run writes to a new timestamped folder here.
builds/

# Local scratch renders at the repo root.
/*.pdf
/*.png

# Python
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Local scratch renders from earlier sessions. Tracked deliverables are only
# the three named PDFs directly under exports/.
previews/
exports/prototypes/
exports/review/
exports/local/
exports/0*.pdf

# Keep the folder so documented compile commands work on a fresh clone.
!builds/.gitkeep
